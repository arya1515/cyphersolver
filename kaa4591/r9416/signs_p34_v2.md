# R9416 f.263 (images _P3, _P4) -- sign inventory, v2

Supersedes `signs_p34.md` for the split signs; all other codes unchanged (see that file). Transcription `p34_v2.txt`.
Readings are from the provisional key (German, one sign per letter, `io` = one sign = e).

| code | shape | reads | P3 | P4 | notes |
|---|---|---|---|---|---|
| z | tall zig-zag "7"/ꟻ with a crossbar and long descender | e | 156 | 125 | the commonest e; was code f |
| f | short dagger Ŧ / ǂ (T with a crossbar) | o | 59 | 48 | was code f; e.g. P4 `HfQ` uon, `QfOHyDO` not(durft) |
| j | looped ƒ (f with a loop at the head) | z | 27 | 15 | was code f; always in `jH` zu, `jqmAzQ` zaigen |
| H | double-post H | u | 75 | 73 | |
| K | H with extra stroke/curl, Ӿ | k | 29 | 18 | kunig, kummen, krieg, kaiser, turken |
| s | small o with a tick, σ | a | 62 | 43 | 6 old `o` re-read as s on P3 (1, 3, 4) |
| O | ⊕ / θ with bar | t | | | not split: ⊕ and barred θ both read t |
| e | θ with tail | ? | 2 | 2 | kept apart from O (P3 21 `esm`, P4 9/11 `AioesQ`, `gse`) |

Counts are signs in cipher lines (brackets and strikes excluded). Old f = 449 (251 + 198) now = z 282 + f 107 + j 43
Old H 155 = H 148 + K 47 minus old K 44 (H→K 3 net; the old transcription already had K 44 and
many H/K were swapped both ways).

Unsplit but suspected: `y` covers the γ and a looped ρ-like form; on P4 (e.g. 29 `yiobzQ`, 32 `AioDzb`) the ρ-form
may be a second e-homophone. `n` vs `u`, `4` vs `z` also worth a check.
